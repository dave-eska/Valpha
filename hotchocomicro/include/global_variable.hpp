#pragma once

#include <string>
#include <vector>

#include <json/value.h>

inline float PIXELS_PER_METER = 50.0f;
inline bool isDebugging;
inline bool isTyping;
inline std::vector<std::string> chatlog;
inline Json::Value config;
